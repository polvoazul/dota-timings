# Dota 2 Timings Audio Generator

You will need ffmpeg!

Install python dependencies:

    pip install -r requirements.txt
    pip install PySimpleGUI

Put some audios in `audios_example` directory.

Then run `python gui.py`
